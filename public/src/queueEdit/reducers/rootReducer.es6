/**
 * @file 队列编辑页主reducer
 * @author Franck Chen(chenfan02@baidu.com)
 */

import {combineReducers} from 'redux';

// reducers

// combine
export default (state = {}, action) => {
    switch (action.type) {
        default:
            return state;
    }
};
